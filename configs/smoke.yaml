seed: 20260813
generator_version: topology-v1
device: auto
output_dir: artifacts
hidden_dim: 128
batch_size: 32
data_workers: 4
prefetch_batches: 4
learning_rate: 0.001
weight_decay: 0.0001
grad_clip: 1.0
max_anchors: 10000
min_anchors: 10000
eval_every: 1000
checkpoint_every: 5000
val_size: 1000
id_test_size: 1000
ood_test_size: 1000
perturb_tolerance: 0.02
# Retained only for manifest compatibility. Direct perturbation uses one pass.
perturb_attempts: 1
ranking_margin: 0.05
lambda_rank: 1.0
lambda_stat: 1.0
coverage_update_every: 1000
coverage_min_count: 100
coverage_js_threshold: 0.10
stability_evals: 3
stability_delta: 0.002
no_progress: false
